# TableBells — Project Memory

**Read this file first in any new session.** It exists to answer three questions fast — what's already built, what's being worked on right now, and what comes after that — without requiring a full re-read of `ARCHITECTURE.md`, `DECISIONS.md`, `RULES.md`, and `PRD.md`. Those four documents are the deep reference; this one is the pointer into them. When something below references a section number, go read that section before writing code against it — this file summarizes, it does not replace.

**Repository state, stated plainly, and the single most important fact in this file:** `package.json`/`tsconfig.json`/`tailwind.config.ts`/`lib/firebase/{client,admin}.ts` now exist (§2 step 1, closed), and `middleware.ts` + the full guest session-boot chain (`slug.service.ts` → `session.service.ts` → `mint-guest-session.ts`) are real, working Admin SDK code (§2 steps 4–5, closed) — but **none of it has ever actually been run.** There is still no `node_modules` in this workspace and no Node/npm available in the environment these passes were built in. Nothing has been compiled, type-checked, or rendered by an actual Next.js process at any point in this project's history. Every claim below about what "works" means "is structurally and logically correct, verified by hand (brace/paren balance, careful re-reading against the architecture docs and the actual existing code)" — not "has been executed." The first real `npm install && npm run typecheck` this project ever undergoes should be treated as a genuine unknown, not a formality.

**Most recent pass (2026-09-09) — the staff-security sweep, closed end to end.** All three consoles (KDS, Cashier, Waiter Floor) now run on the real `tb_staff` session; the mock PIN layer is fully purged (`lib/cashier/mock-staff.ts`, `components/cashier/pin-lock-screen.tsx`, `components/waiter/pin-challenge-modal.tsx`, `components/waiter/open-floor-toggle.tsx` all **deleted**, zero importers confirmed first). A real staff-member seeder exists (`scripts/seed-staff-member.ts`, `npm run seed:staff`, `tsx` dev-dep). The Waiter Floor's per-action step-up PIN challenge and Open Floor Mode were **removed by an explicit business-logic decision** — DECISIONS.md **ADR-5** — which also redrew the void boundary (waiter: draft-cart edits only; cashier/manager: sent-line void with a mandatory reason, written to an indefinitely-retained audit trail). DECISIONS.md (ADR-5), ARCHITECTURE.md (§2.5 role list + the §7.2 console/staff subtree of the directory tree — the *only* part of that tree reconciled with the real build) are updated to match. Still never run — same caveat as everything else.

**Then (2026-09-09, second pass) — the Cashier Dashboard is live-wired.** `hooks/use-live-cashier-data.ts` (new) opens real `onSnapshot` listeners on `tables` / `sessions` / `orders` / `staffAlerts` for the branch; `MOCK_TABLES`/`MOCK_ORDERS`/`MOCK_ALERTS` are gone from the Cashier surface (`mock-alerts.ts` deleted). The ADR-5 line-void now acts on real order documents.

**Then (2026-09-09, third pass) — the Waiter Floor is live-wired.** `hooks/use-live-waiter-data.ts` (new) — narrower budget than the Cashier: `tables` + `sessions` + `orders where status == 'ready'` only, **no** `staffAlerts`. The shared snapshot converters were extracted to `hooks/live-snapshots.ts` (`convertTableSnapshot`/`convertSessionSnapshot`/`convertAlertSnapshot`/`toMillisMaybe`/`SessionWithId`), imported by both console hooks. `lib/cashier/mock-tables.ts` **deleted** (last importer gone; `src/lib/cashier/` is now empty). Route note: the Waiter Floor lives at `/{tenantSlug}/floor` — there is no `/waiter` route.

**Then (2026-09-09, fourth pass) — the KDS ticket-detail view is live-wired.** `hooks/use-live-order.ts` (new) is a single-doc `onSnapshot` on `orders/{orderId}` (`'not_found'` is a first-class state). `kds/[orderId]/page.tsx` passes `tenantId`/`branchId`/`orderId`; `ticket-detail-view.tsx` reads live. Because a live read makes local `setOrder` mutations wrong, the detail screen's Accept/Bump/Return-to-Prep and Void-line handlers now call the **already-existing** `advanceTicket` / `voidTicketLine` actions (no new actions, no fan-out) — this is finally `voidTicketLine`'s KDS caller. `handleGhostReport` stays a local boolean (no `staffAlerts` writer exists). **`lib/kds/mock-orders.ts` deleted.**

**Then (2026-09-09, fifth pass) — the Stock Catalog is live-wired, and ALL mock data is now gone.** `hooks/use-live-catalog.ts` (new) is the staff-side twin of `use-live-menu.ts`: same two reads (`menuPublished/v{n}` + `live/availability`), `menuVersion` resolved server-side via `server/services/menu-version.ts` (`resolveMenuVersion`, extracted from `guest-boot.service.ts`). Unlike the guest hook it keeps the rich `CatalogItem` shape and returns *every* active item with a computed `available` flag (item + per-modifier-option) so the Stock Board can show 86 state. The **KDS Stock Board** and the **Waiter order-entry menu** both consume it. `toggleStock` is now a **real server action** (`server/actions/kds.actions.ts`) — cookie-verified, `canToggleStock` (kitchen/manager/owner), whole-doc transactional write to `live/availability`; the Stock Board's toggles call it and the listener reflects the change (no local `setCatalog`). **`lib/kds/stock-catalog.ts` deleted — `grep "export const MOCK_" src/` now returns nothing.** `src/lib/kds/` holds only `sla-status.ts` (pure utils); `src/lib/cashier/` is empty.

**Then (2026-09-09, sixth pass) — the Waiter can pick a party and send a REAL order.** `waiter-menu-entry.tsx` gains a **party picker** (one party → auto; zero → "guest must scan the QR"; 2+ → chips), and `submitStaffOrder` (mock) is replaced by **`placeStaffOrder`** (`server/actions/staff-order.actions.ts`). **DESIGN REVERSAL — DECISIONS.md ADR-6:** instead of the long-planned `placeStaffOrder → orders` direct write, it writes an **`orderRequests`** doc (Admin SDK, so the `isGuest` create rule doesn't apply) tagged `placedBy: { kind: 'staff', uid }`, and the existing `priceOrderRequestTrigger` → `priceOrderRequest` pipeline prices it — one price authority, no duplicated money math. `priceOrderRequest` gained a small `placedBy.kind === 'staff'` branch (skip party-membership check; stamp the ticket's `placedBy` honestly). The client watches the `orderRequests` doc briefly for a pricing rejection (86'd item, bad modifier) and keeps the waiter on the cart to fix it.

**Then (2026-09-09, seventh pass) — Waiter Floor ops loop closed.** (1) **Manually Open Table** — `openTableSession` (`staff-order.actions.ts`) lets a waiter open a tab for a table with 0 parties (guest with no phone, ordered verbally). It runs the SAME session write as a QR scan: the party-creation half of `resolveGuestSession` was extracted to **`createPartySession(tx, …)`** in `session.service.ts` and is now called by both, flagged `source: 'staff'` + a new `GuestSession.openedByStaffUid`. (2) **Mark as Served** — `markOrderServed` (`kds.actions.ts`, a thin wrapper over `advanceTicket`'s `ready → served` — cookie-verified, `checkTransition` role gate, `servedAt` + `events`, idempotent). The Waiter Ready-Tickets panel's `servedLocally` mock is gone; the row leaves the live `status == 'ready'` query on its own, with an `optimisticallyServed` set bridging the tap→delta gap.

**Then (2026-09-09, eighth pass) — Bill lifecycle + Table PIN backend (DECISIONS.md ADR-7).** (1) **Table join PIN** — `GuestSession.joinPin` (4-digit, CSPRNG via `node:crypto` `randomInt`, minted once in `createPartySession` for QR *and* staff parties, never mutated). It is the planned key for a future PIN-gated "join this existing tab" flow — the settled answer to the "QR-scan divergence": a scanner still gets their OWN party (multi-party per table is a REQUIRED feature — communal tables, split checks), and joining an existing bill will require entering that party's PIN. **Guest PIN-entry UI + the join action are NOT built** — backend prep only. (2) **`requestBill`** (`bill.actions.ts`, `canHandleBilling` = server/cashier/manager/owner) — one txn: flip `session.status` → `billing` + stamp `billingRequestedAt` (idempotent), and write a `staffAlerts` doc `type: 'bill_request'` (`StaffAlertType` gained the value) on EVERY call (re-notify). (3) **`printBill`** — increments `session.printCount` (+ `lastPrintedAt`), sessions `active`/`billing` only, returns `duplicate: true` when `printCount` was already > 0. (4) **`resolveStaffAlert`** — flips a `staffAlerts` doc to `resolved` (+ `resolvedByUid`/`resolvedAt`); backs "Dismiss". (5) **`use-live-waiter-data` regained its `staffAlerts` listener** (dropped in the 3rd pass) — the Waiter needs `bill_request` rows. (6) Shared **`components/console/party-bill-actions.tsx`** (Request Bill / Print Bill, duplicate-flagged) wired into the Cashier `TableDetailPanel` (new `branchId` prop) per party AND the Waiter `PartyPicker` (new `sessions` prop threaded from `waiter-floor-view`). (7) `alerts-inbox.tsx` now branches on `alert.type` (bill_request → teal + Dismiss; ghost → amber + canExecuteBan). New `waiter-alerts-strip.tsx` shows bill_request rows on the floor. (8) The old per-table "Print Receipt" `setTimeout` mock in `table-detail-panel.tsx` is **removed** — per-party `printBill` replaces it; physical ESC-POS `/api/print/[billId]` (ADR-2 Layer 5) stays deferred. New predicate `canHandleBilling` in `staff-permissions.ts`. Still never run.



**Then (2026-09-09, thirteenth pass) — Localization + Fast Lock + Order Attribution (DECISIONS.md ADR-11).** THREE targets. **(1) Localization:** new `branches/{b}.settings = {currency, vatPpm, receiptFooter}` (`BranchSettings` type; `vatPpm` = VAT parts-per-million integer, 50_000=5%, NO float in money path). **`splitInclusive(grossFils, vatPpm=50_000)`** — the ONE VAT fn now takes the rate as a param. `priceOrderRequest` reads `branch.settings.vatPpm`/`.currency` and **SNAPSHOTS both onto every `Order`** (`order.currency`, `order.vatPpm`) — a settings change never re-prices an existing ticket (same principle as ADR-8). `voidTicketLine` recomputes with `order.vatPpm`. New `lib/format/money.ts` (`formatMoney(minor, currency)`; `formatAed` = wrapper), `lib/branch-settings.ts` (defaults + %⇄ppm), `server/actions/branch-settings.actions.ts` (`updateBranchSettings`, `canManageSettings` manager/owner), `hooks/use-branch-settings.ts`, `/{tenantSlug}/manager/settings` + `store-settings-view.tsx`. Guest currency threaded via `GuestSessionContext.currency` (resolved in `guest-boot.service.ts` → new `resolveBranchSettingsFor` in `menu-version.ts`). **Migrated to `formatMoney`:** guest menu/cart/checkout, KDS `ticket-card`/`ticket-detail-view`, Cashier `table-detail-panel`. **NOT migrated (cosmetic, documented):** `table-card`, `settle-close-control`, `menu-maker-view`, `stock-item-row`, `waiter-menu-entry` menu prices — no order/context currency source; still show AED. Money MATH is fully currency/rate-correct. `order.service.ts` + `pricing.service.ts` are the dual-compiled files — changes are additive/import-free, `functions/` untouched. **(2) Fast Lock:** new `components/console/lock-switch-button.tsx` — `signOut(auth)` (best-effort) then `lockTerminal`; prominent "Lock / Switch User" pill replacing the small "Lock" in Cashier + Waiter headers (removed their `useTransition`/`startLock`). **(3) Order Attribution:** `placeStaffOrder` writes `placedByName: staffSession.displayName`; guest checkout gets an OPTIONAL `guestName` field (asked at checkout, NEVER at QR scan — `firestore.rules` `orderRequests` create `keys().hasOnly` gained `'guestName'` + `safeText(…,60)`). `priceOrderRequest` copies both to `order.placedByName`/`order.guestName` (sanitised) and stamps `session.guestName` on first use. KDS ticket + Cashier panel show the guest name chip + "· by <staff>". `Order` gained `currency`/`vatPpm`/`placedByName`/`guestName`; `GuestSession` gained `guestName: string|null` (`createPartySession` writes null); `OrderRequest` gained optional `guestName`/`placedByName`. Still never run.

**Then (2026-09-09, twelfth pass) — Manager Staff Management (DECISIONS.md ADR-10).** `/{tenantSlug}/manager/staff` → `components/manager/staff-manager-view.tsx` (fetch-on-mount + refetch, NO live listener — keeps `pinHash` out of the browser). **`server/actions/staff.actions.ts`** (`canManageStaff` = manager/owner, cookie-verified, Admin SDK): `listStaff` (→ `StaffMemberSummary[]`, `where branchIds array-contains branchId`, no hash) and `upsertStaff` (create → derived uid `mbr_<tid>_<staffCode>`, PIN argon2id-hashed via `hashStaffPin`, full §1.6 doc; edit → patch name/staffCode/role/branches/override/status, optional PIN re-hash + lockout clear). **KEY DECISIONS:** role is **single-select** (task asked for multi — but cookie/claims/rules/`staff-login` are all single-`role`, and `manager` ⊇ cashier/server); identifier is **`staffCode` + PIN**, not email (per-user-salted argon2id needs a fast pre-PIN identifier); **owner-only** guards on creating an `owner` / granting `overrideAuth` / editing an owner; a manager can only ADD branches they hold (keep/drop existing is fine). **Revoke** = `status` → `inactive`/`suspended` (blocks next `/lock` login — `staff-login` already checks `!== 'active'`) + best-effort `adminAuth.revokeRefreshTokens` + `setCustomUserClaims(uid,{stf:false})` (kills a live session's listeners in ~1h). **Residual gap:** the `tb_staff` cookie (8h, no denylist) still passes server-action auth until expiry. New types `StaffMember`/`StaffMemberSummary`/`StaffMemberStatus` in `types/firestore.ts` (`status` widened to add `'inactive'`); `staff-login.service.ts`'s local `MemberDoc.status` now uses the shared type (behaviour unchanged). New `canManageStaff` predicate. `firestore.rules` unchanged. Seed script stays as the first-owner bootstrap. Still never run.

**Then (2026-09-09, eleventh pass) — Manager Table Management + QR export (DECISIONS.md ADR-9).** `/{tenantSlug}/manager/tables` → `components/manager/tables-manager-view.tsx` + `qr-print-sheet.tsx`; hook `hooks/use-live-tables.ts` (single-collection listener). **`server/actions/table.actions.ts`** (`canManageTables` = manager/owner, cookie-verified, Admin SDK): `upsertTable` (create → mint slug via new `generateTableSlug()` in `slug.service.ts` + write `tables/{id}` full doc + root `tableSlugs/{slug}` mapping in one txn; edit → only code/label/zoneId/seats/status), `rotateTableSlug` (§8.1 rotation — new slug, old mapping `active:false`, bump `slugVersion`). `TableSlugDoc` promoted to `types/firestore.ts`. New `canManageTables` predicate. **KEY DECISION — QR URL:** the task asked for `https://[domain]/[tenantSlug]/[branchSlug]?table=[tableId]`; that is the §8.1 semantic-URL anti-pattern (enumerable, leaks tenant+tableId). **Built instead: `${NEXT_PUBLIC_APP_URL}/t/{opaque 12-char slug}`** — the real guest boot URL. New env var `NEXT_PUBLIC_APP_URL` (fallback `https://app.tablebells.ae`). New unverified deps `qrcode-generator` + `@types/qrcode-generator` (pure JS, browser-safe, chosen over `qrcode` for no `fs`). QR sheet = `window.print()` + `@media print` isolation, vector SVG. `restaurantName` now read best-effort from `tenants/{tid}.displayName ?? .name`. `disabled` status does NOT deactivate the slug (`checkGuestBoot` already handles `disabled`). Still never run.

**Then (2026-09-09, tenth pass, chronologically after Settle & Close below) — Manager Menu Maker (DECISIONS.md ADR-8).** First `components/manager/` + `manager/` route surface. New **`menuDraft/current`** branch doc (`MenuDraftDoc`, owner/manager-read-only in `firestore.rules`, Admin-SDK write) holds the editable menu TREE. **`MenuTreeCategory` = `{ id, name: LocalizedText, sortIndex, items: MenuItem[] }`** — the `name` closes the "raw category id shown to guests" gap; `publishMenu` writes it onto published categories too, and `use-live-menu.ts` / `use-live-catalog.ts` now read `category.name.en` (raw-id fallback kept for pre-ADR-8 docs). Three actions in **`server/actions/menu.actions.ts`** (all `canManageMenu` = manager/owner, cookie-verified, server-side re-validation/sanitisation): `seedMenuDraft` (from live `menuPublished/v{n}` or blank), `saveMenuDraft` (replace whole tree, last-write-wins), `publishMenu` (one txn: re-validate, reject `EMPTY_MENU`, write `menuPublished/v{max(branch.menuVersion, draft.lastPublishedVersion)+1}`, `set(branch, {menuVersion}, {merge})`, stamp `draft.lastPublishedVersion`). New: `hooks/use-menu-draft.ts` (single-doc listener, `'missing'` first-class), `components/manager/menu-maker-view.tsx` (category name/sort inputs, item add/edit modal, Save/Publish bar — Publish disabled while dirty), `app/(console)/[tenantSlug]/manager/menu/page.tsx` (`canManageMenu` gate → denied panel for cashier/server/kitchen), `canManageMenu` in `staff-permissions.ts`. `middleware.ts` matcher gained `manager`. **Version-bump-on-publish is deliberate** (price stability mid-session; guest/waiter pick up new menu on next load — same as `menu-version.ts` already documents). NOT built: modifier-group editing (passed through untouched), images, drag-reorder (numeric `sortIndex` field), normalised `menuItems` collection. Still never run.

**Then (2026-09-09, ninth pass) — Settle & Close, the core operational loop is CLOSED.** `closeSession` (`bill.actions.ts`, `canHandleBilling`, Admin SDK, one txn): session → `status: 'closed'` + `closedAt`/`closedBy`/`closeReason: 'settled_by_staff'`; strips this session's entry from `tables/{tableId}.parties[]` and recomputes `partyCount`/`openTabFils`/`status` (emptied table → `available`, unless `disabled`/`attention` — mirror of `createPartySession`); resolves this session's still-open `bill_request` alerts (ghost alerts left for a manager). UI: new **`components/cashier/settle-close-control.tsx`** — a Cashier-only 2-tap confirm (shows the tab total), one per party in `TableDetailPanel`, shown while session is `active`/`billing`. On success nothing local happens — the `tables` listener drops the party row and the component unmounts. **No payment gateway** (out of scope) — assumes cash/card taken on a standalone machine; `paymentStatus`/`bills/{billId}` untouched (no bill doc is created anywhere yet). Still never run.

---

## 1. What Is Completed

**The entire Phase 1 UI is now built, end to end, across all four surfaces the product needs — guest ordering, kitchen, cashier, and waiter floor.** Every surface is structurally complete and internally consistent; every surface also runs entirely on mock data and simulated transports, cataloged precisely in §4. "Completed" here means "correctly built against the architecture," not "production-ready" — those are different claims, and this file has kept them separate the whole way through.

### Documentation (repo root)

- **`ARCHITECTURE.md` v3.0** — the full technical and security blueprint, including the §1.8 schema patch and, as of this pass, a formalized `live/availability` shape (`AvailabilityDoc`, `types/firestore.ts`) covering both item- and modifier-option-level toggles.
- **`DECISIONS.md`** — four accepted ADRs: two-collection split (ADR-1), layered printer sanitization (ADR-2), the probe-attack ceiling fix (ADR-3), the `SameSite=Lax` continuous-cart cookie (ADR-4).
- **`RULES.md`** — the binding coding constitution: stack constraints, security boundaries, mobile-first-guest vs. tablet-first-ops UI standards, code-quality law.
- **`PRD.md`** — Phase 1 scope: three pillars (Continuous Cart Guest UI, KDS, Cashier Dashboard), each with its bounded minimum enabling capability. The Waiter Floor View (§1, below) is new since PRD.md was written — it emerged mid-build from a real operational constraint (PIN-per-order is too slow at rush volume), not from PRD.md's own pillar list; it fits under "Continuous Cart Guest UI"/"Cashier Dashboard"'s existing scope rather than constituting a new pillar.

### Backend Security — `firestore.rules`

Unchanged since last written: guest/staff split enforced via `sign_in_provider`, the two-collection split enforced at the rules layer (`noMoneyFields()` + `keys().hasOnly()`), the party-ownership IDOR gate, the ghost-ban gate (UID + device), `isCashierBillUpdate()`'s field-scoped write path, default-deny.

### Backend Services — `order.service.ts` + `pricing.service.ts`

Unchanged in substance: `priceOrderRequest` as sole price authority, deterministic-hash idempotency, Layer 2 sanitization, the corrected (ADR-3) probe-attack ceiling, `splitInclusive()` as the only VAT arithmetic in the codebase. **The `AvailabilityDoc` 3-way drift is now reconciled** — `order.service.ts`'s local shape matches the canonical `types/firestore.ts` one field-for-field (two maps, `unavailableItems`/`unavailableModifierOptions`, Timestamp-typed here vs. number-typed there, the same server/client convention already used for `OrderRequestDoc.createdAt`). ARCHITECTURE.md §2.4's own inline example was patched to match. **This closed a real gap, not just a rename**: `resolveAndPriceLines` now checks a selected modifier option against the fast 86 broadcast path too (`unavailableModifierOptions`), not only the slower `menuPublished` snapshot's `opt.available` — before this pass, a modifier 86'd seconds ago during a rush could still be ordered until the next menu republish. **`order.service.ts` is now relocated** into `src/server/services/order.service.ts` (this pass) — its Admin SDK import was fixed to `adminDb` from `lib/firebase/admin.ts`, via a RELATIVE path rather than the `@/*` alias every sibling file uses, deliberately: this file is now compiled twice, once by Next.js's bundler and once by a plain `tsc` for the `functions/` package, and plain `tsc` does not rewrite path aliases — see that file's own header for the full reasoning. `pricing.service.ts` was already relocated during the KDS pass.

### Guest Frontend — Menu & Cart/Checkout

`app/(guest)/t/[slug]/{layout,page}.tsx`, `cart/page.tsx`, `components/providers/cart-provider.tsx`, `components/guest/{menu-item-card,cart-bar,cart-checkout-view}.tsx`. Mobile-first, coral theme. **Both guest pages are now fully live-wired** — see the Live Data subsection below for the checkout pass. `lib/guest/resolve-table-placeholder.ts` — kept deliberately through the menu-page pass because `cart/page.tsx` still needed it — is now **deleted**: `cart/page.tsx` was its last consumer, confirmed via grep (zero remaining imports) before removal.

### Live Data — Project Foundation, Bouncer & Vault, Session Brain

The project's real runtime foundation, `middleware.ts`, and the guest session-boot chain, all built and reviewed across several passes:

- **Foundation**: `package.json`, `tsconfig.json` (strict), `tailwind.config.ts` (three semantic token groups defined, not yet applied to components — see §4), `lib/firebase/client.ts` (SSR-guarded, App Check gated behind `NEXT_PUBLIC_RECAPTCHA_SITE_KEY`), `lib/firebase/admin.ts` (Application Default Credentials only, no service-account key files — ARCHITECTURE.md §8.6).
- **The Bouncer & The Vault**: `server/auth/device-cookie.ts` (Edge-compatible `jose` JWT sign/verify for `tb_did`, payload `{did}` only — `tid` deliberately dropped, a device is not tenant-scoped), `server/auth/mint.ts` (device-token minting, Edge), `middleware.ts` (Edge runtime, matcher `['/t/:path*', '/j/:path*', '/:tenantSlug/(kds|cashier|floor)/:path*']` — **corrected here**: an earlier draft of this section called this file "Node runtime"; it is Edge, deliberately, specifically so it can use `jose` instead of a Node-only JWT library pulling in `firebase-admin`). Console routes actively clear a stray `tb_did` if present, defending against this workspace's single-origin dev setup lacking production's subdomain separation.
- **Session Resolution & The Security Vault**: `server/services/slug.service.ts` (`resolveTableSlug` — the real Admin SDK read of root-level `tableSlugs/{slug}`, uniform-failure collapsing "never existed" and "rotated away from" into one result), `server/services/session.service.ts` (`checkGuestBoot` — ban/tenant/table checks; `resolveGuestSession` — the full §3.3 wake/move-confirm/party-creation algorithm, one Firestore transaction, now formalizes `GuestSession` in `types/firestore.ts` for the first time), `server/auth/mint-guest-session.ts` (`mintGuestSessionToken` — Firebase Auth custom-claim minting, now accepts an externally-supplied `uid` so the Firebase Auth identity and a session document's `hostUid` are guaranteed to match — see §4's now-closed watch item on this).
- **Guest Menu & Cart/Checkout, both live-wired**: `server/services/guest-boot.service.ts` (`bootGuestPage` — the shared slug→cookie→boot→resolve→mint sequence, extracted once a second page needed the identical chain, per RULES.md §4.9), `components/guest/guest-boot-states.tsx` (the shared `<GenericUnavailable>`/`<TableFull>`/`<MoveConfirm>` components both guest pages render), `components/providers/guest-session-provider.tsx` (signs in client-side with the minted custom token, exposes session context), `hooks/use-live-menu.ts` (two live listeners replacing the old hardcoded menu array), `cart-provider.tsx` (now also owns `clientRequestId` — stable across a retried submit of the same cart, rotates the instant `lines` actually change), `cart-checkout-view.tsx` (a real `addDoc` into `orderRequests` + a real `onSnapshot` listener on the result, per DECISIONS.md ADR-1 — see §4 for the one real gap this doesn't close).

### Kitchen Display System — Queue, Ticket Detail, Stock Board

- **Ticket queue — LIVE-WIRED, and now behind REAL staff authentication.** `kds/page.tsx`, `kds-queue-view.tsx`, `ticket-card.tsx`, `station-filter.tsx` — station-filtered, tablet grid, dark palette, a single shared `ClockProvider` tick driving every SLA badge. `hooks/use-live-orders.ts` holds the real `orders where status in ['new','prep','ready'] orderBy placedAt limit 100` listener (ARCHITECTURE.md §2.3), including its own Timestamp→number conversion. `handleAdvance` calls the real `advanceTicket` Server Action (`server/actions/kds.actions.ts`), backed by a real, exhaustive transition-legality matrix (`server/services/ticket-state.service.ts`, all five rows from §2.2's table; only `new→prep`/`prep→ready` have a real caller yet). **The caveat flagged at the end of the previous pass is now closed**: `advanceTicket` no longer accepts a client-supplied `actor` at all — it reads and independently verifies the real staff session cookie itself (see the new Staff Authentication subsection below), the same way a Server Action, being a real network endpoint any caller can hit directly, has to verify its OWN caller rather than trust whatever a rendering page happened to pass down as props. `resolveStaffContextPlaceholder` is deleted; it was KDS's only consumer.

### Staff Authentication & PIN Verification

Real, and now consumed by **every** console surface — not the ENTIRE three-factor design ARCHITECTURE.md §1.6 describes (the terminal device-secret factor is still unbuilt; see the explicit gap below).

- **Console-wide rollout (2026-09-09).** Cashier (`cashier/page.tsx` → `cashier-dashboard-view.tsx`) and Waiter Floor (`floor/page.tsx` → `waiter-floor-view.tsx`) now call `requireStaffSession(tenantSlug)` server-side exactly like `kds/page.tsx`, and pass a real `StaffIdentity` (derived via `toStaffIdentity`, `lib/console/staff-permissions.ts`) down to the client view. Neither renders a PIN pad anymore — `/{tenantSlug}/lock` + `middleware.ts` own terminal unlock. "Lock" on both clears the real cookie via the `lockTerminal` server action (`server/actions/staff-session.actions.ts`).
- **`lib/console/staff-permissions.ts`** — the REAL role predicates, replacing the ones that lived beside `mock-staff.ts`: `canExecuteBan` (manager/owner + overrideAuth, unchanged), `canVoidSentLine` (cashier/kitchen/manager/owner — everyone except `server`, per ADR-5), plus `StaffIdentity`/`toStaffIdentity`/`roleLabel`. Client-importable (`lib/`, no `server/` dependency), used both by client components (courtesy) and by `bill.actions.ts` (authority) — the "check it more than once" discipline.
- **`server/auth/staff-session-cookie.ts`** — `StaffSessionPayload` now also carries `displayName` (signed + strict-validated), so every console can render "who is logged in" without a per-page `members/{uid}` read. NOT added to the Firebase custom claims (`firestore.rules` has no use for it). **Consequence:** any `tb_staff` cookie minted before this change fails verification and forces a re-login — moot today (nothing deployed), but real if a stale cookie ever exists.
- **`scripts/seed-staff-member.ts`** (`npm run seed:staff`, `tsx`) — the staff-onboarding / PIN-reset tool that was flagged missing. Hashes a plaintext PIN with the real `hashStaffPin`, writes a `tenants/{t}/members/{uid}` doc matching ARCHITECTURE.md §1.6 field-for-field, reusing the one Admin SDK singleton. `--reset-pin` and `--dry-run` modes. Needs ADC (`gcloud auth application-default login`) — documented in `scripts/README.md`. Never run.

Original build notes (still accurate):

- **`server/auth/staff-pin.ts`** — real argon2id (`hash-wasm`, a WASM implementation chosen specifically to avoid the native-binding deployment risk the more common `argon2` npm package carries in serverless contexts — unverified, since no Node runtime exists in this environment to actually benchmark it against). **A schema correction, not a silent deviation**: ARCHITECTURE.md §1.6 listed `pinHash`/`pinSalt` as two fields; the standard argon2id PHC-encoded string is self-contained (salt embedded), making a separate `pinSalt` field redundant. Patched in ARCHITECTURE.md; `pinSalt` is not written or read anywhere.
- **`server/auth/mint-staff-session.ts`** — mints the Firebase custom token carrying `{ stf, tid, role, bids, overrideAuth }`, matching `firestore.rules`' `isStaff()`/`inBranch()` exactly. Reuses a STABLE uid (the `members/{uid}` document id) rather than minting fresh per login — meaning every login after the first hits the identical `auth/uid-already-exists` race `mint-guest-session.ts` was fixed for two passes ago; built idempotent from the start this time, having learned that lesson.
- **`server/auth/staff-session-cookie.ts`** — a second, Edge-compatible (`jose`) signed cookie (`tb_staff`, 8-hour expiry, its own `STAFF_SESSION_SECRET` — deliberately separate from `DEVICE_COOKIE_SECRET`), verifiable by `middleware.ts` without ever touching `firebase-admin` or argon2. This is the answer to "does argon2 need an Edge-compatible equivalent": no — hashing only ever happens once, in the Node-runtime `/api/auth/pin` route, at login; every subsequent console request is gated by `middleware.ts` verifying this ALREADY-MINTED cookie's signature on Edge, the identical pattern `tb_did` already established.
- **`server/services/staff-login.service.ts`** — orchestrates lookup-by-staff-code, the 5-strikes/15-minute lockout (race-safe, inside a transaction), argon2id verification, and claims minting. Staff code required before PIN: real per-user-salted argon2id cannot support "type only a 4-digit PIN, the system figures out who you are." `components/console/staff-login-view.tsx` (staff code → PIN, POST `/api/auth/pin`) is now the **only** staff login UI in the codebase.
- **`app/api/auth/pin/route.ts`** — Node runtime (the default for Route Handlers; explicitly not Edge). Uses the same fixed placeholder tenant (`tb_0492`) every mock dataset in this project already uses for its lookup — see the real, separately-flagged gap below on why.
- **`app/(console)/[tenantSlug]/lock/page.tsx` + `components/console/staff-login-view.tsx`** — the real login screen ARCHITECTURE.md §7.2 named since the first architecture pass, built for the first time. No Stitch design reference exists for it; styled plainly. Includes an open-redirect guard on the `?next=` param (rejects both absolute and protocol-relative URLs).
- **`middleware.ts`** — now also verifies `tb_staff` on every console route, redirecting to `/{tenantSlug}/lock` (carrying `?next=`) when missing or invalid; `/lock` itself is exempt from that redirect to avoid looping. `tb_did` cookie hygiene on console routes is unchanged.
- **`server/services/resolve-staff-session.ts`** — the Server-Component-side re-verification `kds/page.tsx` calls, mirroring `guest-boot.service.ts`'s "don't just trust that middleware ran" discipline.
- **Ticket detail — LIVE-WIRED (2026-09-09, fourth pass).** `kds/[orderId]/page.tsx` → `requireStaffSession` → `<TicketDetailView tenantId branchId orderId tenantSlug>`; `ticket-detail-view.tsx` reads via `useLiveOrder` (single-doc `onSnapshot`, `hooks/use-live-order.ts`). Accept/Bump call `advanceTicket`; the `ready → prep` back-step maps `'prep-return'` → `advanceTicket({to:'prep'})`; per-line Void calls `voidTicketLine` (its first KDS caller). No local `setOrder` — the listener is the source of truth, same as the queue. Ghost-order **report** is still a local boolean (no `staffAlerts` writer action yet). `void-line-dialog.tsx` (dark) and `ghost-order-report-dialog.tsx` unchanged.
- **Stock Board — LIVE-WIRED (2026-09-09, fifth pass).** `kds/stock/page.tsx` (resolves `menuVersion`, passes `tenantId`/`branchId`/`menuVersion`), `stock-board-view.tsx` (`useLiveCatalog` + real `toggleStock`), `stock-item-row.tsx`, `kds-nav-tabs.tsx`. Searchable, category-grouped, item- and modifier-option-level toggles — both write `live/availability` (`unavailableItems` / `unavailableModifierOptions`) through the one `toggleStock` action. `menuState/{itemId}` (§2.4's *durable* per-item record, alongside the broadcast) has no schema anywhere in the codebase, so it's **deferred** — same scoping as `voidTicketLine`'s fan-out.
- **The most important thing this surface got right, twice over**: `flagGhostOrder` (ARCHITECTURE.md §8.7 — permanent ban, token revocation, account deletion) is manager/owner + `overrideAuth` only, never kitchen. The KDS ghost-order dialog is a **report**, not an execution — it creates a `staffAlerts` entry (`type: 'ghost_suspected'`, a value this build introduced) for a manager to review elsewhere. This distinction is re-enforced, independently, in the Cashier Dashboard (below) and again in the Waiter Floor View (below) — three surfaces, one authority boundary, checked at every one of them rather than assumed from the first.

### Cashier Dashboard — LIVE-WIRED (2026-09-09)

`cashier/page.tsx`, `cashier-dashboard-view.tsx`, `table-overview-grid.tsx` + `table-card.tsx`, `alerts-inbox.tsx`, `execute-ban-dialog.tsx`, `table-detail-panel.tsx`, `components/cashier/void-line-dialog.tsx`, **`hooks/use-live-cashier-data.ts` (new)**. Light utilitarian palette. **Real staff identity** (`requireStaffSession` → `StaffIdentity` prop) **and real data**: `useLiveCashierData(tenantId, branchId)` opens four `onSnapshot` listeners — `tables` (orderBy `sortIndex`), `sessions` (`status in [active,idle,billing]`, single-field `in`, no index), `orders` (`status in [new,prep,ready,served]` + orderBy `placedAt`, reuses the existing composite index; adds `served` to the KDS trio so a delivered-but-unbilled line can still be voided), `staffAlerts` (`status == open`, sorted client-side). `MOCK_TABLES`/`MOCK_ORDERS`/`MOCK_ALERTS` and every seeded `useState` array are gone from this surface; `lib/cashier/mock-alerts.ts` is **deleted** (zero importers). `cashier/page.tsx` now passes only `tenantId`/`branchId`.

- **Grid** renders from live `tables.parties[]` (the denormalised copy `resolveGuestSession` writes).
- **Detail panel** renders parties from `tables.parties[]`, and where a live `sessions` doc matches on `sessionId` it shows the real `runningTotals` net/VAT split + order/item counts + `risk.score`; falls back to `party.openTabFils` with no session doc.
- **Void a sent line (ADR-5)** — `canVoidSentLine`-gated "Void" on real order lines (filtered by `order.tableId === table.id`) → real `voidTicketLine`. No more local `splitInclusive` recompute or `ORDER_NOT_FOUND` special-case — the `orders` listener reflects the write, exactly like KDS's `handleAdvance`. Only genuine rejections surface in a status line.
- **Execute ban** — still the sole `flagGhostOrder` surface, still gated three times, still a **mock consequence**. Since alerts are now a live listener, a reviewed alert is hidden via a local `dismissedAlertIds` set until real `flagGhostOrder` writes `status: 'resolved'`.

Print Receipt is still a labeled Layer-5 mock (`DECISIONS.md` ADR-2).

### Waiter Floor View — LIVE-WIRED (2026-09-09)

`floor/page.tsx` (route: `/{tenantSlug}/floor`, **not** `/waiter`), `waiter-floor-view.tsx`, `ready-tickets-panel.tsx`, `waiter-menu-entry.tsx`, **`hooks/use-live-waiter-data.ts` (new)**. Reuses the Cashier's `TableCard`/`TableOverviewGrid`. **Real staff identity** (`StaffIdentity` prop) **and real data**: `useLiveWaiterData(tenantId, branchId)` — `tables` (orderBy `sortIndex`; carries `status:'attention'` + `parties[].riskFlagged` for the grid), `sessions` (`status in [active,idle,billing]`), `orders` **`where status == 'ready'` only** (single-field equality, no index; sorted FIFO by `readyAt`/`placedAt` client-side). No `staffAlerts` — the waiter has no inbox. Converters shared via `hooks/live-snapshots.ts`.

- **Floor grid** — live `tables.parties[]`; "attention" tables and risk-flagged parties render from real fields.
- **Take an order** (`waiter-menu-entry.tsx`) — **LIVE (ADR-6).** Party picker (`parties[]` off the live `tables/{id}` denorm) → `placeStaffOrder` → `orderRequests` (`placedBy: staff`) → the real pricing pipeline → `orders`. Draft-cart review strip (qty ±, remove) is still a waiter's *entire* void authority. A pricing rejection is surfaced (staff-readable) and keeps the waiter on the cart. A table with **0 parties** shows a **"Manually Open Table"** button (`openTableSession`, seventh pass) — creates a `source: 'staff'` session so a phone-less guest can be served.
- **Ready for Pickup** (`ready-tickets-panel.tsx`) — renders `live.readyOrders`. **"Mark as Served" is LIVE (seventh pass)** — `markOrderServed` (`ready → served`, `kds.actions.ts`); the served row leaves the `status == 'ready'` query on its own, with an `optimisticallyServed` set bridging the tap→delta gap and dropping back on a rejection. Only **"Report Suspected Ghost Order"** here is still local-state (no `staffAlerts` writer action yet).
- **Void a sent line** — not on this terminal at all (`canVoidSentLine` false for `server`). ADR-5's `pin-challenge-modal.tsx` / `open-floor-toggle.tsx` stay deleted.

### Shared infrastructure, grown across all four surfaces

- `components/providers/clock-provider.tsx` — one skew-corrected 1Hz tick, shared by every screen with a live timer (KDS SLA badges, Cashier's table-elapsed grid).
- `lib/time.ts` — `getElapsedSec`/`formatElapsed`, extracted from `lib/kds/sla-status.ts` once Cashier needed the same math for something that isn't an SLA; `sla-status.ts` now re-exports both so nothing that already imported from it broke.
- `types/firestore.ts` — grown from just `Order` (KDS) to also cover `MenuItem`/`ModifierGroup`/`ModifierOption` (Stock Board), `Table`/`TableParty` (Cashier), `StaffAlert`, and `AvailabilityDoc`. `OrderLineVoid.reason` gained `'server_error'` (ADR-5), kept in lockstep BY HAND with `lib/console/void-reasons.ts` (which moved out of `lib/kds/` now that Cashier voids too).
- `lib/console/staff-permissions.ts` + `server/actions/{staff-session,bill}.actions.ts` — the real staff role predicates and the `lockTerminal` / `voidTicketLine` server actions, shared across Cashier and (for `staff-permissions`) Waiter. `mock-staff.ts` is **deleted**.

---

## 2. What Is Active Next

**The Live Data & Firebase Plumbing Phase.** Wiring the UI placeholders built across §1 to real Firestore listeners, implementing `middleware.ts`, and securing the device-cookie JWTs — the founder's own framing for this phase, stated exactly.

Concretely, in the order this phase likely has to tackle them (each later item assumes the ones before it exist):

1. ~~Project scaffolding~~ — **closed.** `package.json`, `tsconfig.json`, `tailwind.config.ts`, `lib/firebase/{client,admin}.ts` all exist. `next.config.ts` was never separately needed — Next.js 15's App Router runs with zero required config for everything built so far.
2. **Three theme token sets**, all still raw hex today, none tokenized — **still open, untouched this pass.** The guest coral palette, KDS's dark ops palette, and the Cashier/Waiter light ops palette. `data-surface="guest"`/`"ops"` CSS custom properties (ARCHITECTURE.md §7.3) plus the matching `tailwind.config.ts` theme extension. Every raw `bg-[#...]` across every component becomes its semantic class here, in one pass, not piecemeal — including the two new guest components this pass added (`guest-session-provider.tsx` has none; `guest-menu-view.tsx` inherits the same raw hex the menu page always had).
3. ~~`lib/firebase/client.ts`~~ — **closed.**
4. ~~`middleware.ts` + `server/auth/mint.ts` + `server/auth/device-cookie.ts`~~ — **closed.**
5. ~~`server/services/slug.service.ts` and `session.service.ts`~~ — **closed**, including the full §3.3 wake/move-confirm/party-creation algorithm (`resolveGuestSession`), not just the boot check.
5a. **Guest Menu surface live-wired.** `app/(guest)/t/[slug]/page.tsx` runs the real chain (now via the shared `bootGuestPage`, see 5c) and hands a custom token down to `GuestSessionProvider` (`components/providers/guest-session-provider.tsx`), which signs in client-side and exposes tenant/branch/table/session context. `useLiveMenu` (`hooks/use-live-menu.ts`) replaces the old hardcoded `MENU_ITEMS` array with two live listeners — `menuPublished/v{n}` and `live/availability` — matching ARCHITECTURE.md §2.4's stated "2 reads" guest menu-load budget exactly (`menuVersion` itself is resolved server-side, once, rather than kept as a third live listener). Rendering lives in a client component, `components/guest/guest-menu-view.tsx`, since a live listener's loading/ready/error states are inherently client-side. **Still not done**: the restaurant's own display name is hardcoded; menu category display labels don't exist in the canonical schema at all (`MenuPublishedCategory` has only an `id`, so the live menu shows raw category ids like `"coffee"`) [**CLOSED tenth pass, ADR-8** — `MenuTreeCategory.name`]; no modifier-selection UI exists anywhere, so every submitted line's `modifierOptionIds` is currently always `[]` — a menu item with a `required: true` modifier group cannot actually be ordered correctly yet.
5b. **Guest Cart/Checkout surface live-wired.** `cart/page.tsx` runs the same real chain and wraps `<CartCheckoutView>` in `<GuestSessionProvider>`. `cart-checkout-view.tsx` does a real, ADR-1-compliant `addDoc` into `orderRequests` (`noMoneyFields`/`keys().hasOnly` at the rules layer, deterministic `clientRequestId`-based idempotency owned by `cart-provider.tsx`, `createdBy` read live from `auth.currentUser.uid` rather than trusted from context) followed by a real `onSnapshot` listener on the resulting document, with proper unmount cleanup and a bounded "still processing" fallback state. Rejection codes (`order.service.ts`'s `reason` field — `CALLER_BANNED`, `RATE_LIMITED`, `ITEM_NOT_FOUND`, …) are mapped to a small, deliberately coarse set of guest-safe messages, never shown verbatim (uniform failure, same principle as session boot). Cart-line/note editing is disabled for the duration of a submission, closing a real race the `clientRequestId` design would otherwise have left open. **The one real gap this cannot close on its own**: `functions/src/triggers/price-order-request.ts` (item 6, below) still doesn't exist, so a real, valid `orderRequests` document is now created — but nothing will ever flip its `status` away from `'pending'` until that trigger is deployed. The "still processing" fallback exists specifically for that gap, not as a permanent design element.
5c. **Shared boot logic, extracted once a second page needed it.** `server/services/guest-boot.service.ts`'s `bootGuestPage(slug)` now owns the full `resolveTableSlug` → cookie verify → `checkGuestBoot` → `resolveGuestSession` → `mintGuestSessionToken` sequence; both `page.tsx` and `cart/page.tsx` call it rather than each carrying their own copy (RULES.md §4.9). `components/guest/guest-boot-states.tsx` does the same for the `<GenericUnavailable>`/`<TableFull>`/`<MoveConfirm>` markup. **A known, accepted inefficiency this introduces**: `resolveGuestSession`'s full transaction now runs independently on every guest-page navigation (menu → cart, or a reload), not once per visit — harmless in the common case (a same-table re-scan just resolves to another 'resumed' wake), and not optimized away this pass since caching the boot result across navigations is a separate, real change to how session state is threaded through the guest surface.
6. ~~`functions/src/triggers/price-order-request.ts`~~ — **closed.** A genuinely new, separate `functions/` npm package now exists (`functions/package.json`, `functions/tsconfig.json`, `functions/README.md` — the last of which explains the build wiring in full, including the somewhat unusual `rootDir: ".."` and its nested compiled-output layout). `priceOrderRequestTrigger` (`functions/src/triggers/price-order-request.ts`) is real, matching the wiring comment `order.service.ts` had carried since before this file existed. A root `firebase.json` was also created (didn't exist before this pass, needed regardless of which Firebase config gets deployed first). **Still missing, deliberately not fabricated**: `.firebaserc` (needs a real project id) and `functions/node_modules` (no `npm install` has ever run here — see the environment caveat repeated throughout this file). **A terminology correction worth recording**: the task that requested this named the target status `'accepted'`; the real, already-implemented, already-consumed-by-`cart-checkout-view.tsx` status value is `'priced'` (with `'rejected'`/`'duplicate'` alongside it) — used throughout, not changed to match the request, since changing it would have broken the guest-side listener built in the previous pass.
7. **The staff server actions.** Real and called from the surface that needs each: `advanceTicket` (KDS queue + detail), `markOrderServed` (Waiter ready panel — thin wrapper over `advanceTicket`'s `ready→served`), `voidTicketLine` (Cashier + KDS detail), `lockTerminal`, `toggleStock` (Stock Board — `live/availability` only, `menuState` deferred), `placeStaffOrder` + `openTableSession` (Waiter — ADR-6 / seventh pass), and (ADR-7) `requestBill` / `printBill` / `resolveStaffAlert` / `closeSession` in `bill.actions.ts` — the first real `staffAlerts` WRITER (`type: 'bill_request'`), and `closeSession` is the first session-close + table-denorm-cleanup writer. **The guest ordering → kitchen → serve → bill → settle → close loop is now end-to-end real code** (never run). **The only mock write actions left:** `security.actions.ts` (`flagGhostOrder`, `unbanIdentity`) and the KDS **ghost-order report** (still a local boolean — a `staffAlerts` `type: 'ghost_suspected'` writer for it does not exist; `requestBill` only writes `bill_request`). **`voidTicketLine`'s §2.5 fan-out beyond the order doc is deliberately not built** (`bills/{billId}` gross, `session.runningTotals`, the guest `item_voided` alert).
8. **Real staff auth — CLOSED for what this project builds now.** ~~substantially closed~~ → all three consoles consume the real `tb_staff` session; `mock-staff.ts` + the three mock PIN components **deleted**; real seeder script exists (`scripts/seed-staff-member.ts`). The Waiter step-up PIN challenge and Open Floor Mode were **removed by decision** (DECISIONS.md ADR-5), not left as debt. **What remains unbuilt is genuinely separate work, not this milestone:** the terminal device-secret factor (ARCHITECTURE.md §1.6's third leg — no `devices/{deviceId}` flow), per-device rate limiting, tenant-slug→tenant-id resolution for the console (`/api/auth/pin` still uses the `tb_0492` placeholder), and ~~staff CRUD~~ (**CLOSED twelfth pass, ADR-10** — `manager/staff` + `staff.actions.ts`; the seeder is now just the first-owner bootstrap).
9. ~~**Cross-page/cross-device state**~~ — **DONE.** Every console surface (KDS queue + detail, Stock Board, Cashier, Waiter Floor + order entry) runs on live Firestore listeners and reflects the others in real time. **There is no mock data left in the app** — `grep "export const MOCK_" src/` returns nothing.

---

## 3. The Backlog (after the Live Data phase)

With all of Phase 1's named UI surfaces built, the backlog is what's left once the plumbing above is real:

- **Manager Console** — menu item CRUD (the Stock Board toggles *availability* against a real `menuPublished` doc now; **creating/editing** an item, its price, its modifier groups, and the `publishMenu` flow that writes `menuPublished/v{n}` still have no UI — nothing in this project writes that doc yet, so the Stock Board / Waiter menu / guest menu all render whatever seed data exists at that path), **staff management** (`scripts/seed-staff-member.ts` is a hand-run stopgap), and reporting. PRD.md named "Menu & Modifier Management" as KDS's minimum supporting capability — the availability half shipped; the CRUD + publish half did not.
- **Payment gateway activation**, the **Superadmin Fleet Console**, and everything else PRD.md §3 explicitly excludes from Phase 1 — Staff Scheduling, Advanced Inventory Management, Customer Loyalty Programs. If a future session reaches for one of these because "the schema already supports it," that's the exact trap PRD.md's opening paragraph exists to name.

---

## 4. Open Corrections / Watch Items

### Historical — already fixed, kept visible so the pattern stays legible

- The staff-approval ceiling originally exempted a session's first order (`orderCount === 0`), trivially defeated by a cheap probe order. Fixed — `DECISIONS.md` ADR-3. Any future reintroduction of a session-history exemption here is a regression, not a refinement.
- `ARCHITECTURE.md` §1.8's canonical order schema once fell out of sync with the actual code and rules. Fixed by a full field-by-field patch; the general lesson is `RULES.md` §4.10 now.
- **Guest device cookies (`tb_did`) were bare, unsigned UUIDs.** Fixed — `server/auth/device-cookie.ts`'s real signed `jose` JWT, enforced on every `/t/*` request by `middleware.ts`.
- **Three watch items logged at the end of the Session Brain build — all three closed this pass**, before any frontend wiring began, exactly as requested:
  1. `mint-guest-session.ts`'s `hostUid`/Firebase-Auth-uid mismatch — `mintGuestSessionToken` now accepts an optional `uid`, and `resolveGuestSession` always supplies one, so the two are guaranteed to agree. **A second, related bug was caught and fixed in the same pass, not separately flagged for later**: with an externally-supplied `uid` now possible, a session *wake* (reusing an already-created `hostUid`) made `adminAuth.createUser({ uid })` throw `auth/uid-already-exists` unconditionally — every single wake would have failed. Fixed by treating that one error code as the expected, idempotent case.
  2. The missing `sessions` composite index (`deviceIds` array-contains + `status` in + orderBy) — added to a newly-created `firestore.indexes.json`. **Scope note**: this file now exists but contains only this one index — ARCHITECTURE.md §1.11 documents at least one other (`sessions`: `joinedUids` ARRAY, `status` ASC) that is still only documentation, not deployable config. Not backfilled here; flagged so it isn't mistaken for complete. **Field-name correction**: the task requesting this index named the orderBy field `updatedAt` — no such field exists on a session document; indexed on the real field, `lastActivityAt`, instead.
  3. `tables.parties[]` being written directly by `resolveGuestSession` rather than by the `syncTableParties` Cloud Function ARCHITECTURE.md §3.6 names — acknowledged, not changed, as an accepted temporary deviation: that Cloud Function doesn't exist yet, and without this direct write the Cashier Dashboard's table grid would simply never reflect a real party. Revisit once `syncTableParties` is actually built — at that point this direct write either becomes redundant (safe to leave, since both would converge on the same state) or should be removed in favor of the trigger, a decision for whoever builds it.

### Open — the accumulated mock/placeholder debt the Live Data phase (§2) exists to close

- **Simulated transports** — all consoles read live. Real *writes*: guest checkout, `advanceTicket`, `markOrderServed`, `voidTicketLine`, `toggleStock`, `placeStaffOrder`, `openTableSession`, `lockTerminal`. Still simulated writes: the ghost-order report (→ a `staffAlerts` writer), `flagGhostOrder` (Cashier execute-ban mock-dismisses locally).
- ~~**`mock-staff.ts`'s plaintext PIN directory is not a security pattern**~~ — **CLOSED (2026-09-09).** `lib/cashier/mock-staff.ts` is deleted; real role predicates live in `lib/console/staff-permissions.ts`, real identity comes from the `tb_staff` session. All 8 former importers migrated, zero remaining confirmed by grep before deletion.
- **Three un-tokenized color palettes**, not one — guest coral, KDS dark, Cashier/Waiter light — all raw Tailwind arbitrary-value hex today. Step 2 in §2 is what closes this, in one pass across all three, not as separate per-surface cleanups.
- ~~**Cross-surface state sync**~~ — **fully resolved.** Every console surface is on live listeners; no mock data anywhere.
- ~~`order.service.ts` was never relocated into `src/`~~ — **closed this pass**, alongside the Cloud Functions trigger that finally needed it as a real caller. See §1/§2 item 6.
- ~~`order.service.ts`'s local `AvailabilityDoc` type has drifted from the canonical one~~ — **closed this pass**, and the real gap it was masking (modifier-level 86 status not checked against the fast broadcast path) is closed with it. See §1's Backend Services entry for the full account.
- ~~The KDS ticket queue is live, but its `advanceTicket` Server Action trusts a claimed, not verified, staff identity~~ — **closed this pass**. `advanceTicket` now independently verifies the real staff session cookie itself and derives `tenantId`/actor from it — never from a client-supplied parameter. See §1's new Staff Authentication subsection.
- ~~`kds/page.tsx` has no real way to resolve `tenantId`/`branchId`~~ — **`tenantId` closed** (comes from the verified staff session cookie's `tid` claim now). **`branchId` is a real, named simplification, not fully closed**: a session carries `bids`, an array of every branch a member is authorized for; `kds/page.tsx` always uses `bids[0]`. No branch-switcher UI exists — a genuinely multi-branch manager is silently pinned to their first authorized branch.
- **NEW — the terminal device-secret factor ARCHITECTURE.md §1.6 specifies is still entirely unbuilt.** A real PIN login now exists (staff code + argon2id-verified PIN), which is genuinely more secure than the plaintext dictionary it replaced — but ARCHITECTURE.md's own framing is explicit that a PIN is only "worthless alone... a second factor," with the THIRD factor being a registered terminal's own device secret (`devices/{deviceId}.secretHash`). No `devices/{deviceId}` registration flow exists anywhere in this codebase. Until it does, a stolen-but-still-valid `tb_staff` cookie is usable from any browser, and per-DEVICE rate limiting (ARCHITECTURE.md's other stated requirement, alongside per-member) isn't implemented either — only per-member lockout is. This is a genuinely separate, substantial feature (device provisioning, secret issuance/rotation), not something this pass silently declined to finish.
- ~~**`pin-lock-screen.tsx` (Cashier) was NOT updated**~~ — **CLOSED (2026-09-09).** `pin-lock-screen.tsx` deleted along with `pin-challenge-modal.tsx` and `open-floor-toggle.tsx`. `components/console/staff-login-view.tsx` (staff code → PIN) is the only staff login UI; Cashier and Waiter consume the resulting `tb_staff` session directly.
- ~~**`mock-staff.ts` is NOT deleted**~~ — **CLOSED (2026-09-09).** Deleted. Cashier's `alerts-inbox`/`execute-ban-dialog`/`cashier-dashboard-view` and the Waiter files now import `canExecuteBan`/`canVoidSentLine`/`StaffIdentity` from `lib/console/staff-permissions.ts` and take a real `staff` prop.
- **STILL OPEN — no tenant-slug-to-real-tenant-id resolution for the staff console.** `tenants/{t}` has no `slug` field; `api/auth/pin/route.ts` still uses the `tb_0492` placeholder. (The Waiter *step-up* verify endpoint that would also have needed this was scrapped with ADR-5, so this is now purely a login-route concern.) Real fix: a `slug` field, a subdomain, or the login form asking for the tenant's own `code`.
- ~~**no staff-member seeding/PIN-reset tooling exists**~~ — **CLOSED (2026-09-09).** `scripts/seed-staff-member.ts` (`npm run seed:staff`, `tsx` dev-dep) hashes a plaintext PIN via `hashStaffPin` and writes a real `members/{uid}` doc; `--reset-pin` and `--dry-run` modes; `scripts/README.md` documents the ADC prereq. Never run (no Node here). **`scripts/**/*.ts` is now inside `tsconfig.json`'s `include`, so `npm run typecheck` will check it too.**
- **NEW — `tsx` is an unverified new dev dependency.** Added to `package.json` for `npm run seed:staff`, never installed. The seed script uses relative imports (`../src/...`), not the `@/*` alias, specifically because `tsx` does not resolve `paths` without extra config.
- **NEW — `hash-wasm` is an entirely unverified new dependency.** (unchanged) Added to `package.json`, never installed or run. argon2id params (`iterations: 3, memorySize: 64MiB`) are a documented starting point, not benchmarked — tune before shipping.
- **NEW (ADR-5) — `voidTicketLine` writes only the order document + `events` row.** The §2.5 fan-out — `bills/{billId}` gross, `session.runningTotals`, `sessions/{s}/alerts += {item_voided}` (the guest's "AED X off your bill" notification) — is deliberately not built; it needs real session/bill wiring the Cashier surface lacks. Whoever wires the Cashier to live orders must add it, or a void will silently not reach the guest.
- ~~**the Cashier void UI runs against mock order IDs**~~ — **CLOSED (2026-09-09).** The Cashier is live-wired (`use-live-cashier-data.ts`); `voidTicketLine` now targets real order documents and the `orders` listener reflects the write. The local recompute / `ORDER_NOT_FOUND` handling was removed.
- **NEW — every staff console assumes the Firebase client SDK is already signed in.** KDS, Cashier, and now the Waiter Floor all open `onSnapshot` listeners against `db` directly with no auth-ready gate. A staff member with a valid `tb_staff` cookie but no client-SDK session (different browser, cleared IndexedDB) hits `firestore.rules`' `isStaff()` with no `request.auth` → `permission-denied` → the surface's generic error shell. The guest side solved this with `GuestSessionProvider.authReady`; there is still no staff equivalent. A real fix is one shared gate for all three consoles (explicitly out of scope for the live-wiring passes).
- **NEW — `staffAlerts` is sorted client-side, not by index.** `use-live-cashier-data` queries `where('status','==','open')` (single-field, no composite index) and sorts newest-first in JS. ARCHITECTURE.md §2.3 specifies `orderBy('createdAt','desc')`, which would need a `(status, createdAt)` composite index in `firestore.indexes.json`. Deferred: ≤5 open alerts in practice, and the alerts inbox was adjacent-scope for this pass. Add the index when the alerts surface is built out.
- **NEW — session/party timestamps are written as plain `Date.now()` numbers, not `Timestamp`s.** `resolveGuestSession` writes `openedAt`/`lastActivityAt` etc. as JS numbers into `sessions` and `tables.parties[]` — unlike `orders`, where `advanceTicket`/`priceOrderRequest` write `serverTimestamp()`. The shared `toMillisMaybe` (`hooks/live-snapshots.ts`) handles both, but the underlying inconsistency should be reconciled when `server/firebase/converters.ts` is built (all server writers should use `serverTimestamp()`).
- **NEW — the Waiter take-order flow has no party picker.** `waiter-menu-entry.tsx` takes `tableId`/`tableCode` only; the take-order header summarises the table's *first* party's session as context. A table with two+ open parties gives the waiter no way to target a specific one — fine while `submitStaffOrder` is a mock, but real `placeStaffOrder` needs a `sessionId`, so a party selector has to land with it.
- **NEW — `hooks/live-snapshots.ts`** is the shared client-side snapshot→type converter module (`convertTableSnapshot`/`convertSessionSnapshot`/`convertAlertSnapshot`/`toMillisMaybe`/`SessionWithId`). `use-live-cashier-data` and `use-live-waiter-data` both import it; `convertOrderSnapshot` stays in `use-live-orders.ts` (its KDS home) and is reused by `use-live-order.ts` (single-doc, KDS detail) via `DocumentSnapshot.exists()`'s type guard. When `server/firebase/converters.ts` is built, all of this folds into it. `src/lib/cashier/` is now an empty directory.
- ~~**`lib/kds/stock-catalog.ts` is the last mock-data file**~~ — **DELETED (fifth pass).** No `export const MOCK_*` exists anywhere in `src/`. The staff catalog is `use-live-catalog.ts` (`menuPublished/v{n}` + `live/availability`), `menuVersion` from `server/services/menu-version.ts`.
- **NEW — `toggleStock` writes `live/availability` ONLY; `menuState/{itemId}` is deferred.** ARCHITECTURE.md §2.4 names a durable per-item `menuState` record alongside the `live/availability` broadcast, but that collection has no `firestore.rules` match and no `types/firestore.ts` type — inventing it was out of scope. `live/availability` is the part that makes the Stock Board work and reaches guests. Whole-doc transactional `set` (no merge) so a "restore" can actually delete a nested key and a first-ever 86 creates the doc.
- **NEW — `use-live-catalog.ts` sets `CatalogItem.categoryId` from the parent published category, not the item's own field.** `MenuPublishedItem` has no `categoryId` (nesting is structural) — same choice `use-live-menu.ts` makes. It also filters `status` leniently (`!== 'draft' && !== 'archived'`, i.e. missing status = active) where the guest hook is strict (`=== 'active'`); a *published* item with no status is active by definition, and the guest hook can afford strictness because it drops 86'd items anyway.
- ~~**NEW — nothing in this project writes `menuPublished/v{n}`...**~~ **CLOSED (tenth pass, ADR-8).** `publishMenu` (`server/actions/menu.actions.ts`, NOT a Cloud Function — a manager-triggered Server Action) snapshots `menuDraft/current` → `menuPublished/v{n+1}` and bumps `branches/{b}.menuVersion`. `seedMenuDraft` / `saveMenuDraft` maintain the draft. The Manager Menu Maker (`/{tenantSlug}/manager/menu`) is the in-app editor. `toggleStock` remains the only writer to `live/availability`.
- **NEW — `toggleStock`'s `AvailabilityEntry.at` is `Date.now()`** (a number), and it copies pre-existing entries (whose `at` may be a real `Timestamp` from `order.service.ts`'s writer) forward verbatim in the whole-doc rewrite. Mixed representation over time; nothing reads `at`. Same standing `Date.now()` vs `serverTimestamp()` note — `converters.ts` unifies.
- **NEW (ADR-6) — a waiter order counts toward the party's per-session 90-second rate limit** (`maxOrdersPerSessionPer90s`, default 3). A busy waiter entering several orders for one big table could hit `RATE_LIMITED`. Accepted for now (per-tenant configurable); a `placedBy: staff` exemption is a *considered* future change, not silent, because a compromised `tb_staff` terminal would otherwise be unbounded.
- **NEW (ADR-6) — a waiter cannot place an order for a table with no open party.** Sessions are created only by a guest QR scan (`resolveGuestSession`); there is no staff "open a tab" path. `waiter-menu-entry.tsx` shows a message. Pre-scan / staff-initiated sessions are a separate feature if ever needed.
- **NEW (ADR-6) — `waiter-menu-entry.tsx`'s pricing-rejection watch is a bounded ad-hoc `onSnapshot`** inside `handleSend` (not a `useEffect`-managed listener like `cart-checkout-view.tsx`). It self-terminates on a terminal status or a 10s timeout; if the component unmounts mid-wait the listener dangles at most that long, and a `mountedRef` guards the post-await `setState`. Lighter than the guest checkout's ref/effect machinery, deliberately.
- ~~**NEW — `use-live-waiter-data`'s `sessions` listener is now UNUSED by the Waiter UI.**~~ **CLOSED (eighth pass).** `waiter-menu-entry`'s `PartyPicker` now consumes `sessions` (threaded from `waiter-floor-view`) to read each party's `status` / `printCount` for `<PartyBillActions>`.
- **NEW (eighth pass, ADR-7) — guest-initiated "Request Bill" is NOT wired.** Only the staff (`tb_staff`-cookie) path exists — a waiter taps it for the guest. A guest has no staff cookie and `firestore.rules` lets no guest client flip a session to `billing`; the guest path needs a guest-ID-token-verified server action, a pattern this codebase has no precedent for (guests write Firestore directly). Documented in ADR-7 as a follow-up, alongside the whole PIN-entry join flow.
- **NEW (eighth pass, ADR-7) — `GuestSession.joinPin` is generated but nothing consumes it yet.** `createPartySession` mints a 4-digit PIN for every party; there is no guest "join an existing tab → enter PIN" UI and no join server action. Backend prep only. Pre-eighth-pass session docs read back with `joinPin`/`billingRequestedAt`/`printCount`/`lastPrintedAt` `undefined` — every reader uses `?? 0` / `?? null` / a truthiness check; `firestore.rules` needed no change (all ADR-7 actions are Admin SDK).
- **NEW (eighth pass) — `session.service.ts` now imports `node:crypto` (`randomInt`) for `generateJoinPin`.** First `node:`-prefixed import in the file (it already used the `crypto` global for `randomUUID`). Fine for the Next.js server / Admin SDK context this file runs in; noted in case a future move of this file into an Edge/`functions` build path trips on it (`order.service.ts` is the dual-compiled one, not this).
- **NEW (eighth pass) — `alerts-inbox.tsx` MUST branch on `alert.type` now.** `bill_request` gets a teal informational row + "Dismiss" (`resolveStaffAlert`); anything else falls through to the amber ghost-order + `canExecuteBan` rendering. A future `StaffAlertType` value with no branch renders as a ghost-order row — add the branch when the type is added.
- **NEW (eighth pass) — `staffAlerts` still has no `(status, createdAt)` composite index.** `requestBill` writes `createdAt: serverTimestamp()`; both console hooks query `where status == 'open'` (single-field) and sort newest-first in JS. Same deferral as before, now with a real writer feeding it. Add the index when the full alerts page is built.
- **NEW (ninth pass, ADR-7) — a `closed` session is in NO live console listener.** `use-live-cashier-data` / `use-live-waiter-data` both filter `status in ['active','idle','billing']`. There is no "recently closed" / shift-Z view; a shift-close report (backlog) needs its own `closed`-scoped read. Also: `closeSession` writes `tables/{id}.parties[]` + `partyCount` + `openTabFils` + `status` DIRECTLY — same accepted GAP-3 deviation as `createPartySession` (no `syncTableParties` trigger); `openTabFils` in particular has no other maintainer, so the recompute-on-close is the closest thing to a canonical write it gets.
- **NEW (ninth pass) — `closeSession` records no payment state.** `closeReason: 'settled_by_staff'` is the only trace. `paymentStatus` / `PAID_CASH` / `PAID_CARD` (named in `firestore.rules` `isCashierBillUpdate`) and `bills/{billId}` are untouched — no bill document is created anywhere in the codebase yet. When the bill/settlement layer lands, `closeSession` should stamp the bill's `paymentStatus` too.
- **NEW (ninth pass) — `<SettleCloseControl>` lives in `components/cashier/`, NOT `components/console/`** (unlike `<PartyBillActions>`). Closing out is a Cashier-station action; the Waiter handheld does not offer it. The `closeSession` server action still re-checks `canHandleBilling` regardless (which admits `server` — a deliberate server-side allowance, just not surfaced in the Waiter UI).
- **NEW (seventh pass) — `createPartySession` extracted from `resolveGuestSession`.** `session.service.ts` now exports it; `resolveGuestSession`'s CREATE branch AND `openTableSession` both call it (RULES.md §4.9 — the ~50-line `newSession` + `tables.parties[]` write was about to be duplicated). Preconditions stay caller-side (they differ: guest checks `bannedDevices` + returns `table_full`; staff checks role + returns `rejected TABLE_FULL`). GAP (2)/(3) in `session.service.ts`'s header (the `hostUid`↔Firebase-Auth mismatch, and `tables.parties[]` written here instead of by a `syncTableParties` trigger) are unchanged — the helper just relocates them.
- **NEW (seventh pass) — a STAFF-opened session has `deviceIds: []`, so a late-scanning guest at that table gets their OWN party.** `resolveGuestSession`'s device lookup (`where deviceIds array-contains <did>`) won't match it. Merging a phone-arriving guest into a waiter-opened tab is a separate feature. `openTableSession` also has no headcount UI — `guestCount: 1` always (so ticket `covers` = 1 for staff-opened orders).
- **NEW (seventh pass) — a staff-opened session's `hostUid` is still `anon_<uuid>` with no Firebase Auth user.** Same GAP (2) the guest path has — no token is minted for it. `firestore.rules`' `inParty()` would reject a guest client trying to touch it (correct — no guest is in it), and `priceOrderRequest` skips that check for `placedBy: staff`, so the pricing path is fine. If a guest ever needs client access to a staff-opened session, `mintGuestSessionToken` + a join flow is the missing piece.
- **NEW (seventh pass) — `waiter-menu-entry.tsx`'s "Opening…" state can stick on a degraded network.** After `openTableSession` returns `'opened'`, `opening` stays `true` until the live `tables` listener delivers the new party. On a good connection (or `persistentLocalCache` hit) that's ~instant; offline, it hangs on "Opening…". Bounded by component unmount. A timeout fallback is a reasonable follow-up.
- **NEW (seventh pass) — `GuestSession.openedByStaffUid: string | null` added** (non-optional). `createPartySession` writes it; pre-existing session docs read back as `undefined` typed `string | null` (harmless — nothing reads it yet; it's audit/reporting data for "which waiter opened this table").
- **NEW — the KDS detail page has no client-side double-submit guard beyond `useTransition`'s `pending`.** `advanceTicket`/`voidTicketLine` are both idempotent server-side (a re-tap returns `already_there`/`already_voided`, no error), so a `pending`-disabled button is belt-and-braces, not load-bearing — same reasoning `kds-queue-view.tsx` documents for having no guard at all.
- **NEW — `handleExecuteBan` local-dismiss is a stopgap.** With alerts now a live listener, a mock "execute ban" can't resolve the alert doc, so the view hides it in a local `Set`. On reload the alert reappears (its `status` is still `open`). Goes away when `flagGhostOrder` is real.
- **NEW (ADR-5) — `OrderLineVoid.at` is `Date.now()`, not `FieldValue.serverTimestamp()`.** Firestore rejects the server-timestamp sentinel inside an array element and `void{}` lives in `items[]`. The top-level `updatedAt` and the `events` row's `at` still use the real sentinel. `types/firestore.ts` already types `at` as epoch-ms `number`, so this is consistent, but it means a voided line's timestamp is the app-server wall clock, not Firestore's.
- **NEW — `displayName` added to the `tb_staff` cookie payload** (signed + strict-validated). Any `tb_staff` cookie minted before this change now fails `verifyStaffSessionToken` and forces a re-login. Moot today (nothing deployed), real if a stale cookie ever exists. Also: a display name is now low-sensitivity PII sitting in an httpOnly+secure signed (not encrypted) cookie — an accepted tradeoff to avoid a per-page `members/{uid}` read, flagged not hidden.
- **NEW (ADR-5) — indefinite retention is now a stated requirement.** Voided-line audit data and the `orders/{id}/events` spine must be retained ≥5 years (UAE VAT). Any future "archive old orders" / TTL work has to carve these out.
- **NEW (ADR-5) — if per-action manager override is ever wanted again** (e.g. a manager authorising a discount from a waiter's handheld), it is a fresh build: `pin-challenge-modal.tsx` and the plan for `/api/auth/pin/verify` were deleted, not shelved. DECISIONS.md ADR-5's "Alternatives Considered" records why.
- **NEW — a second Firestore composite index gap, closed before it could surface as a runtime error**: the ticket-queue listener's `where('status','in',[...]) + orderBy('placedAt')` needs a composite index Firestore does not auto-create for an `in` + `orderBy`-on-a-different-field combination — same category of gap as the `sessions` lookup two passes ago. Added to `firestore.indexes.json` alongside it.
- **A terminology correction, same pattern as `'accepted'`/`'priced'` last pass**: the task requesting this milestone described KDS statuses as `pending`/`preparing`/`ready`. The real, already-implemented, already-UI-consistent values are `new`/`prep`/`ready` (`ticket-card.tsx`'s own button copy — "Accept · Preparing" — already uses "preparing" as DISPLAY text for the `prep` status, which is exactly why the underlying value was kept as `prep`, not renamed to match the request literally).
- ~~**NEW — menu category display labels do not exist anywhere in the schema.**~~ **CLOSED (tenth pass, ADR-8).** `MenuTreeCategory` (`types/firestore.ts`) has `name: LocalizedText` + `sortIndex`; `publishMenu` writes them onto published categories, and `use-live-menu.ts` / `use-live-catalog.ts` read `category.name?.en?.trim() || category.id` (raw-id fallback only for pre-ADR-8 published docs). `order.service.ts`'s local `MenuPublishedCategory` was left untouched — it never reads category fields, and it is the dual-compiled file.
- **NEW (tenth pass, ADR-8) — a published `menuPublished/v{n}` is NEVER overwritten; `publishMenu` always writes v{n+1} and moves the pointer.** So an already-open guest/waiter `onSnapshot` (subscribed to the fixed `v{n}` doc) does NOT reflect a republish until the page reloads. Deliberate — price stability mid-session, same behaviour `menu-version.ts` documents. Making the hooks track the pointer live is a possible follow-up.
- **NEW (tenth pass, ADR-8) — the Menu Maker is a single denormalised draft doc, not a normalised `menuItems/{itemId}` collection.** `menuDraft/current` is edited whole and published whole. §1.5's `menuItems` collection is still unused. `saveMenuDraft` is last-write-wins (no multi-editor conflict handling). Modifier-group editing is NOT in the UI — existing groups pass through save/publish untouched; `option.available` is forced `true` on write (86 stays `live/availability`'s job).
- **NEW (tenth pass) — `menu.actions.ts`'s `cleanText` scans code points** (drops C0/C1 controls, collapses whitespace) rather than using a control-char regex literal — an earlier draft wrote literal control bytes into the source and made the file "binary" to tooling; the scan avoids that. Light hygiene only (staff-entered text), not `order.service.ts`'s full Layer-2 pass.
- **NEW (tenth pass) — `middleware.ts` matcher gained `manager`** (`(kds|cashier|floor|lock|manager)`). The `manager/menu` AND `manager/tables` pages ALSO enforce their role predicate themselves (`canManageMenu` / `canManageTables` — cookie check alone isn't enough; a logged-in cashier gets a denied panel).
- **NEW (eleventh pass, ADR-9) — the guest QR URL is `${NEXT_PUBLIC_APP_URL}/t/{slug}`, NOT the task's `/{tenantSlug}/{branchSlug}?table={tableId}`.** The latter is §8.1's enumerable-semantic-URL anti-pattern. If a future task references that URL shape, it's wrong — the opaque `tableSlugs/{slug}` chain is load-bearing. `generateTableSlug()` (`slug.service.ts`) mints; `upsertTable`/`rotateTableSlug` (`table.actions.ts`) write `tables/{id}` + `tableSlugs/{slug}` (both Admin-only in rules).
- **NEW (eleventh pass) — `NEXT_PUBLIC_APP_URL` env var added** (`.env.local.example`), fallback `https://app.tablebells.ae`. Only consumer so far: the Manager Table QR exporter. A wrong/unset value prints QR codes pointing at the wrong origin — fine for previewing the layout, not for a real print run.
- **NEW (eleventh pass) — `qrcode-generator` + `@types/qrcode-generator` are unverified new deps** (`package.json`, never installed). Pure JS, no `fs` — deliberately chosen over `qrcode` for browser-bundle safety. `qr-print-sheet.tsx` uses `qr.createSvgTag({scalable:true})` + `dangerouslySetInnerHTML` (markup is locally generated from our own URL string — trusted).
- **NEW (eleventh pass) — `upsertTable` create computes `sortIndex` from a NON-transactional max-scan** of existing tables; two simultaneous creates can tie (cosmetic — the grid just `orderBy`s it). Also: a `disabled` table keeps its slug active (`checkGuestBoot` returns `table_unavailable` for `status==='disabled'`, so no reprint needed on re-enable); only `rotateTableSlug` ever sets a `tableSlugs` doc `active:false`.
- **NEW (eleventh pass) — restaurant display name** is now read best-effort from `tenants/{tid}.displayName ?? .name ?? tenantSlug` in `manager/tables/page.tsx` — the FIRST real source for the "display name is hardcoded" gap, but only on that page; the guest surface still hardcodes it.
- **NEW (twelfth pass, ADR-10) — staff revocation is not truly instant.** `status` → non-active blocks the NEXT login; `adminAuth.revokeRefreshTokens` + `setCustomUserClaims(uid,{stf:false})` (best-effort in `upsertStaff` on any role/branch/status/override change) kill a LIVE session's Firestore listeners within ~1h. The `tb_staff` cookie (standalone 8h JWT, no denylist) still passes server-action auth until it expires. A cookie revocation list is the fix — separate work.
- **NEW (twelfth pass) — staff roles stay SINGLE.** The Staff Management UI is a single-select. If a future task asks for multi-role, that's a cascade through `StaffSessionPayload`, the Firebase custom claims, `firestore.rules` (`role in [...]`), `staff-login.service.ts`, and every `StaffPermissionSubject` predicate. `manager` already covers cashier/server abilities in every `canX`.
- **NEW (twelfth pass) — `upsertStaff` privilege guards:** only `owner` can create/edit an `owner` or grant `overrideAuth`; a manager can only ADD branches in their own `bids` (keeping/dropping existing branches on a member is allowed, so a branch-A manager can still revoke a member who is also in branch B). `listStaff` returns sanitised `StaffMemberSummary` (NO `pinHash`) — the roster is a fetch, not a live listener, specifically to keep hashes server-side.
- **NEW (thirteenth pass, ADR-11) — VAT rate is now per-branch (`branches/{b}.settings.vatPpm`, parts-per-million integer).** `splitInclusive(gross, vatPpm=50_000)` takes it as a param; `priceOrderRequest` snapshots `currency`+`vatPpm` onto each `Order`; `voidTicketLine` recomputes with `order.vatPpm`. A settings change NEVER re-prices an existing order (snapshot-at-price-time). If a future task hardcodes 5% or AED anywhere in the money path, that's a regression.
- **NEW (thirteenth pass) — currency display is PARTIALLY wired.** `lib/format/money.ts` `formatMoney(minor, currency)` is the new formatter. Migrated: guest menu/cart/checkout (currency from `GuestSessionContext`), KDS ticket-card/detail + Cashier table-detail-panel (currency from `order.currency`). NOT migrated (still literal AED, cosmetic only): `cashier/table-card.tsx`, `cashier/settle-close-control.tsx`, `manager/menu-maker-view.tsx`, `ops/stock-item-row.tsx`, `waiter/waiter-menu-entry.tsx`. Fix = a shared console currency context/prop. `formatAed` still exists as a `formatMoney(x,'AED')` wrapper for those.
- **NEW (thirteenth pass) — `guestName` on `orderRequests` needs the `firestore.rules` `keys().hasOnly` list to include it** (done). It's optional — the create rule accepts the key present OR absent. `order.service.ts` re-sanitises via `sanitizeGuestText(…, 60)`. Guest name is asked at CHECKOUT only, never at QR scan (ADR-4 friction rule).
- **NEW (thirteenth pass) — `GuestSessionContext` gained `currency`/`vatPpm`**, resolved server-side in `guest-boot.service.ts` via new `resolveBranchSettingsFor` (`menu-version.ts`) — one extra branch read on guest boot. Same "resolved once, not live-listened; picked up on next reload" trade-off as `menuVersion`.
- **NEW (twelfth pass) — `StaffMemberStatus` widened to `'active' | 'inactive' | 'suspended'`.** `staff-login.service.ts` blocks any `!== 'active'` (unchanged logic, type now shared from `types/firestore.ts`). `StaffMember` / `StaffMemberSummary` are new canonical types (the §1.6 shape the seed script already wrote).
- **NEW — the guest page's `'move_pending'` and `'table_full'` states have placeholder-only UI.** Both are real, reachable outcomes of `resolveGuestSession` now that it's wired in, but neither has a Stitch design reference the way the rest of the guest surface does. Currently rendered as plain, undesigned text inline in `page.tsx` — functionally correct, visually a stand-in.
- **STILL OPEN, restated so it isn't lost**: the ~6-hour guest session lifetime (ARCHITECTURE.md §1.4) has no enforcement mechanism anywhere in the chain built so far — `mint-guest-session.ts` flagged this originally, `resolveGuestSession` doesn't touch it either, and `GuestSessionProvider` signs in once on mount with no re-authentication path for an expired token. A real answer likely lives in the session document's own expiry field, checked either by `firestore.rules` or client logic — still nobody's build task yet.
- **NEW — a stale field-name assumption caught while live-wiring checkout, not shipped.** `cart-checkout-view.tsx`'s own previous header specified the real submit implementation down to the exact code, including reading a rejection's detail off `data.rejectionDetail`. The real `order.service.ts` writes the machine code to `data.reason` instead; `rejectionDetail` is only present alongside one specific reason (`ITEM_NOT_FOUND`) and isn't guest-safe to show even then. Caught by re-reading `order.service.ts` directly rather than trusting the earlier spec's field name — a reminder that even a previously-approved "exact code to become real" comment is worth re-verifying against the actual backend before implementing it verbatim.
- ~~Checkout is real, but nothing prices it yet~~ — **closed this pass**: `functions/src/triggers/price-order-request.ts` is now real (§1/§2 item 6). A guest order can, in principle, travel fully end-to-end now — write, price, ticket — though this has never actually been run (no `node_modules` anywhere in this project yet, functions package included). `cart-checkout-view.tsx`'s "still processing" fallback state is no longer covering for a permanently-missing piece, only for genuine cold-start/processing latency or a not-yet-deployed function in a given environment.
- **NEW — `resolveGuestSession`'s full transaction now runs on every guest-page navigation**, not once per visit, since `page.tsx` and `cart/page.tsx` each independently call the shared `bootGuestPage`. Harmless today (a same-table re-scan just re-resolves to the same 'resumed' wake and re-mints a token for the same uid), but worth knowing before assuming session state is threaded through the guest surface any more efficiently than "re-derive it fresh on every page."
- **`lib/guest/resolve-table-placeholder.ts` is deleted**, not just superseded — its last consumer (`cart/page.tsx`) was rewired this pass, confirmed via grep before removal.
